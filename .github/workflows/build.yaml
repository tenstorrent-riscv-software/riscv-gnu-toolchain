name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - tt-master
  pull_request:
    branches:
      - tt-master

jobs:
  build:
    uses: ./.github/workflows/build-reusable.yaml
    with:
      artifact-name: build
